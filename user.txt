在model/py下创建文件 分别是训练一个模型和使用模型进行推理。
我现在要训练一个分类器，模型输入可以看dataset下的train_features文件，一行就是模型的输入，为一段数组的数据特征。
输入经过分类器后输出一个向量 表示该对这段数组进行的具体的操作，具体操作可以看model/util里的delFunc  rangedFunc compressFunc等。
要求你使用的模型经过训练可以很好地对未知数据进行判断使用什么操作来压缩这段数组。
训练完毕后保存模型，以便后面推理时调用。推理时接受一个文件里面存有数组特征 输出一个向量。